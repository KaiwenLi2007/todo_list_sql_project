PROMPT LOG

---

Initialize Database: Connect to a SQLite file named course_manager.db. Create a table tasks with:

  id: Primary Key (Integers).
  course_name: Text (Required).
  deadline: Text/Date (Format: YYYY-MM-DD).
  est_hours: Float (User input for expected duration).
  urgency_score: Text (Generated by system logic).

---

Now, formulate the operational commands (CRUD):

Make CREATE: prompt for course_name, deadline, est_hours; run Urgency Engine; INSERT; notify success and urgency.
---
Make READ: Option A sort by deadline, Option B sort by urgency_score; display formatted table.
---
Make UPDATE: show tasks, ask id and new_est_hours; re-run Urgency Engine; UPDATE tasks by set est_hours, re-calculate urgency_score. 
---
Make DELETE: ask id; DELETE WHERE id = ?; verify and confirm.

Main loop: START → INIT Database → WHILE user_choice != "Exit": display menu 1–5, call corresponding command.

---

Make a Urgency Engine: 
Capture current_datetime and deadline_datetime.
Available_Hours = (Deadline - Current_Time).
Buffer = Available_Hours - est_hours.
IF Buffer < 24h → "1-HIGH".
ELSE IF Buffer < 72h → "2-MEDIUM".
ELSE → "3-LOW".

---

Main Execution Loop:
START program.
INIT Database.
WHILE user_choice != "Exit":
  Display Menu Options (1–6).
  Call corresponding Command.
END program.

---

Make an HTML file so this becomes a web app that only needs to run on my computer.

---

The id for each entry is not updating by itself. Make a much better looking user interface.

---

Make the sorting feature better. The date should be able to identify the date both in the format of March, Mar, 03, 3 and sort them accordingly. Use HTML/CSS if needed.

---

The sorting function still has bugs. The Date can only sort in one direction. The urgency is not reliable in predicting how urgent a task is.

---

The two date sorting options do not have a difference. Delete one.

---

All tasks are being identified as medium. Make it more sensitive.

---

It is not displaying as HIGH.

---

I want to sort this to-do list. The urgency button should update the display so that (HIGH) will go before the (Medium) and then the LOW. The date will sort as which item is the closest to today's date.

---

Make a README.md. Only give titles, I will fill it in later. 

